var commentable = "#<%= @comment.commentable_type.underscore %>-<%= @comment.commentable_id %>";
$(commentable + ' .comments-container').append("<%= escape_javascript(render('comments/show', comments: [@comment], new_comment: @new_comment)) %>");
$(commentable + ' .comments-header h6').html("<%= @total_comments %> Comments");
